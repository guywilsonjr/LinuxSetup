function gcom () {
  echo 'premes'
  msgs=''
  echo 'postemes'
  if [ $# -eq 0 ]
  then
    echo "No arguments supplied"
    return
  fi
  for word in "$@"; do
    msgs="$msgs $word"
  done
  echo "commiting with With Message:\n $msgs"
  git commit -a -m "${msgs%?}"
}
