function gcom () {
  if [ $# -eq 0 ]
  then
    echo "No arguments supplied"
    return
  fi
  msgs="$@"
  for word in "$@"; do
    msgs+="$msgs"
  done
  echo "commiting with With Message:"
  echo $msgs

  git commit -a -m "${msgs%?}"
}
