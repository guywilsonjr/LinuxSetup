function gcom () {
  if [ $# -eq 0 ]
  then
    echo "No arguments supplied"
    return
  fi
  commit_msg=$1
  shift
  for word in "$@"; do
    commit_msg+=" $word"
  done
  #echo "commiting with With Message: $commit_msg"

  git commit -a -m "$commit_msg"
}
