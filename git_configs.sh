git config --global checkout.workers 16
git config --global checkout.thresholdForParallelism 400
git config --global fetch.parallel 8
git config --global http.maxRequests 20
git config --global submodule.fetchJobs 4
#fetch.unpackLimit 500
